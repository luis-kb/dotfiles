# Status bar
set -g status-right '#(t=$(cat ~/.tmux_task 2>/dev/null); [ -n "$t" ] && echo "[task: $t]")'
set -g status-right-length 60
set -g status-interval 5

# Keybinding: set task via prompt inside tmux
bind T command-prompt -p "Task:" "run-shell 'echo \"%1\" > ~/.tmux_task'"
